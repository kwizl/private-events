<h1>Show</h1>
<div>
  <%= @user.name %>
</div>

<div class="container">
  <div class="row">
    <% @user.attended_events do |e| %>
      <%= e.description %>
    <% end %>
  </div>
</div>

<h2>Previously attended events</h2>

<% @upcoming_events.each do |u| %>
  <p><%= u.name %></p>
<% end %>
